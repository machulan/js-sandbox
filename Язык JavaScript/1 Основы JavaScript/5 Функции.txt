function f(a, b, c) {
}

ј–√”ћ≈Ќ“џ ѕќ ”ћќЋ„јЌ»ё
≈сли параметр не передан при вызове Ц он считаетс¤ равным undefined.

‘ункци¤ с return без аргументов или отсутствием return возвращает undefined.

alert(f) // выведет код функции f

Function Declaration Ц функци¤, объ¤вленна¤ в основном потоке кода.
Function Expression Ц объ¤вление функции в контексте какого-либо выражени¤, например присваивани¤.

!!!
‘ункции, объ¤вленные как Function Declaration, создаютс¤ интерпретатором до выполнени¤ кода. (их можно вызвать до объ¤влени¤)
!!!


new Function(params, code)
params //ѕараметры функции через зап¤тую в виде строки
code // од функции в виде строки

var sum = new Function('a,b', ' return a+b; ');


Named Function Expression

var f = function factorial(n) {
  return n ? n*factorial(n-1) : 1;
};
var g = f;  // скопировали ссылку на функцию-факториал в g
f = null;
alert( g(5) ); // 120, работает!

≈сли функци¤ задана как Function Expression, ей можно дать им¤.
ќно будет доступно только внутри функции (кроме IE8-).
