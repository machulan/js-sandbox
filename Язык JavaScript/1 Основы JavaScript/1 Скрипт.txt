script

async
1) первым сработает тот скрипт, который загрузится раньше
2) когда скрипт будет загружен, он выполнится

defer
1) скрипты выполняются по порядку
2) сработает, когда весь HTML-документ будет обработан браузером.

async/defer работают только для внешних скриптов


//СТРОГИЙ РЕЖИМ

"use strict"; // => соответствие ES5 (6)

в IE8 не поддерживается =>
подключить скрипты es5-shim.js и es5-sham.js из https://github.com/es-shims/es5-shim

