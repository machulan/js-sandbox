var names = 'ћаша, ѕет€, ћарина, ¬асилий';

var arr = names.split(', '); // как в python

str.split(sep, n) // => n первых элементов полученного  		// от split(sep) массива

arr.join(';') // не как в python, а наоборот 


ћетоды массивов:

push/pop, shift/unshift, splice Ц дл€ добавлени€ и удалени€ элементов.
join/split Ц дл€ преобразовани€ строки в массив и обратно.
slice Ц копирует участок массива.
sort Ц дл€ сортировки массива. ≈сли не передать функцию сравнени€ Ц сортирует элементы как строки.
reverse Ц мен€ет пор€док элементов на обратный.
concat Ц объедин€ет массивы.
indexOf/lastIndexOf Ц возвращают позицию элемента в массиве (не поддерживаетс€ в IE8-).
ƒополнительно:

Object.keys(arr) возвращает массив свойств объекта.